language:       cpp
before_install:
              - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
              - sudo add-apt-repository ppa:boost-latest/ppa -y
              - sudo apt-get update -qq
install:
              - if [ "$CXX" = "g++" ]; then
              -   sudo apt-get install gcc-4.8
              -   export CXX="g++-4.8" CC="gcc-4.8"
              -   sudo apt-get install libboost1.48-dev libboost-program-options1.48-dev libboost-program-options1.48.0 libboost-test1.48-dev libboost-test1.48.0
              - fi
              - if [ "$CXX" = "clang++" ]; then
              -   sudo apt-get install libboost1.54-all-dev
              - fi
              - sudo apt-get install flex bison libgmp-dev glpk scons
compiler:
              - gcc
              - clang
script:         scons -Q buildExecutables
branches:
  except:       gh-pages
notifications:
  email:
    on_success: change
    on_failure: always
